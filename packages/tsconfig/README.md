# `@repo/tsconfig`

Shared TypeScript configuration package for Reliverse.

## Purpose

Provides reusable TS config presets for workspace packages and apps.

## Exports

- `./ts-only.json`
- `./ts-tsx.json`

## Notes

- Central config package
- Intended to keep TS settings consistent across the monorepo
