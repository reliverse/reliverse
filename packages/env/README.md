# `@repo/env`

Shared environment schema package for Reliverse.

## Purpose

Provides typed environment parsing and exports for different runtime surfaces.

## Exports

- `@repo/env/api`
- `@repo/env/expo`
- `@repo/env/vite`

## Notes

- Built on `arkenv` and `arktype`
- Central place for environment contracts across the monorepo
