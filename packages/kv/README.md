# `@repo/kv`

Shared key-value utilities package for Reliverse.

## Purpose

Provides reusable KV-oriented primitives and helpers for higher-level app and backend code.

## Notes

- Workspace package
- TypeScript-first
- Export surface is rooted at `src/index.ts`
