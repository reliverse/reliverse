import { definePlugin } from "@reliverse/rempts";

export default definePlugin({
  entry: import.meta.url,
  name: "tools-rse-plugin",
  description: "Useful tools Rse plugin for the Reliverse developer ecosystem",
});
