# @reliverse/config-rse-plugin

Rse config command plugin.

## Commands

```bash
rse config generate --apply
```

Generates `rse.schema.json(c)` from loaded Rse plugin schema contributions and creates/updates `rse.config.json` or `rse.config.jsonc` without requiring the config file at runtime.
